# v2 Cycle 2 Implementation Plan — Multi-Hand + Simulated Table Players

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** The table plays like a real casino table — you play 1–3 hands, up to 5 basic-strategy bots (configurable mistake rate) occupy other seats, cards deal in true casino order, only your decisions are graded — per spec `docs/superpowers/specs/2026-07-16-v2-cycle2-table-design.md` (THE authority; task requirements = its sections + this plan).

**Architecture:** Engine-first: `Game` gains a seat model (`seats: Seat[]`) with the v1 `hands`/`active` API preserved as views over the player seat (UI churn minimized). Bots resolve synchronously in the engine (seeded rng, chart-perfect via `basicPlay` with the game's rules, mistake substitution); the UI paces display only. Profiles gain `seats: SeatConfig` (migration default = today's solo table).

**Tech Stack:** unchanged.

## Global Constraints

- Spec sections override this plan; STOP and flag disagreements.
- Bots are count-blind, flat-bet 1u, never take insurance, and produce ZERO GradedEvents.
- Deterministic: same seed + same SeatConfig ⇒ identical transcript including bot mistakes.
- All money math flows only from player hands; bot results are display-only.
- strict TS; full `npm test` + `npm run build` (+ `npm run e2e` where stated) green before every commit; commits end with `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- v1-solo parity: with default SeatConfig {playerHands:1, bots:0}, every existing test passes unchanged.

## Waves

- **Wave 1 (serial, engine):** T1 (=C2.1 SeatConfig/Profile.seats/migration) → T2 (=C2.2 multi-seat dealing + RC) → T3 (=C2.3 bot autoplay + determinism) → T4 (=C2.4 multi-hand player) → T5 (=C2.5 insurance across hands).
- **Wave 2 (parallel ×2, UI):** T6 (=C2.6 bot rendering + paced narration + fast-forward; files Table.tsx/app.css/useGame.ts) ∥ T7 (=C2.8 profile editor Seats section; ProfileEditor.tsx only).
- **Wave 3 (serial):** T8 (=C2.7 multi-hand bet entry + per-hand results; Table.tsx/ActionBar.tsx) → T9 (=C2.9 e2e + screenshots + coordinator review).

---

### Task 1: SeatConfig + Profile.seats + migration (spec "Seat model")
**Files:** Modify `src/engine/game.ts` (types only: export SeatConfig, Seat), `src/store/types.ts` (Profile.seats), `src/store/profiles.ts` (default + backfill in load/migration), tests in `src/store/profiles.test.ts`.
**Produces:** `SeatConfig { playerHands: 1|2|3; bots: 0|1|2|3|4|5; botMistakePct: number; playerPosition: number }`; `Profile.seats: SeatConfig`; `DEFAULT_SEATS = {playerHands:1, bots:0, botMistakePct:0, playerPosition:0}`; existing stored profiles WITHOUT seats load with DEFAULT_SEATS (merge backfill, tested).
- [ ] RED: profile blob without seats → loads with DEFAULT_SEATS; with partial seats → merged. GREEN. Full suite. Commit `feat(c2): seat config + profile backfill`.

### Task 2: Multi-seat dealing + RC visibility (spec "Dealing")
**Files:** `src/engine/game.ts`, `src/engine/game.test.ts`.
**Produces:** `Game.seats: Seat[]` built from cfg.seats (bots at indices ≠ playerPosition); two-pass deal in seat order (each hand of each seat: card1; dealer up; card2s; hole); every non-hole card counts immediately; v1 `hands`/`active` become getters over the player seat's hands (API-compatible); `startRound` still accepts number|number[] (T4 formalizes arrays).
- [ ] RED: rigged shoe, 2 bots + player at position 1 → exact card-to-seat mapping asserted; RC = sum of all visible tags; solo default byte-parity (existing tests untouched). GREEN. Commit `feat(c2): multi-seat dealing`.

### Task 3: Bot autoplay + mistakes + determinism (spec "Bot play")
**Files:** `src/engine/game.ts`, `src/engine/game.test.ts`.
**Produces:** `resolveBotHands(upTo: seatIndex)` — bots before the player seat play out right after deal... casino reality: ALL seats act in order, so bots before the player act first, bots after act when the player's last hand is done, then dealer. Engine resolves each bot seat synchronously at the correct point; `botActionLog: { seat: number; action: Action; card?: Card }[]` for UI pacing; mistakes: with p = botMistakePct/100 per decision (game's seeded rng), pick a uniformly-random OTHER legal action; bots never surrender-as-mistake into illegal states (only legal actions sampled), never insure; bot busts/settles tracked on their Seat for display.
- [ ] RED: mistakePct 0 → 500-round seeded soak, every bot action === basicPlay(cards, up, botCtx, rules); mistakePct 100 → differs whenever an alternative legal action exists; same seed+config twice → identical botActionLog + shoe; RC includes bot hit cards. GREEN. Commit `feat(c2): bot autoplay`.

### Task 4: Multi-hand player (spec "Multi-hand player")
**Files:** `src/engine/game.ts`, `src/engine/game.test.ts`, `src/engine/payouts.test.ts`.
**Produces:** `startRound(bets?: number | number[])` — array length = playerHands; one bet GradedEvent per hand at pre-deal TC; player's hands play left→right with splits inserting within the seat (cap 4 per original hand); settle nets summed into bankroll; per-hand results.
- [ ] RED: 2 hands, distinct rigged outcomes → independent nets, bankroll = sum; bet grading two events; split inside hand 1 doesn't disturb hand 2 ordering; payout audit rows for multi-hand. GREEN. Commit `feat(c2): multi-hand player`.

### Task 5: Insurance across hands (spec "Multi-hand player")
**Files:** `src/engine/game.ts`, `src/engine/game.test.ts`.
**Produces:** single insuranceDecision covers all player hands: stake = sum(bet_i)/2, pays 2:1 on dealer BJ (per-hand even-money math preserved), one insurance GradedEvent; bots unaffected.
- [ ] RED: 2 hands take+BJ → net 0 (non-BJ hands); take+noBJ → −(b1+b2)/2 then play continues. GREEN. Commit `feat(c2): multi-hand insurance`.

### Task 6: Bot seats UI + paced narration + fast-forward (spec "UI")
**Files:** `src/ui/screens/Table.tsx`, `src/ui/app.css`, `src/ui/useGame.ts`.
Compact bot rows (smaller PlayingCard via a size prop or CSS class) labeled P1…P5 around the player area; bot actions replayed from botActionLog paced by dealSpeedMs into the message strip ("P2 hits… P2 busts"); tap anywhere on the strip (or a ⏩ button ≥48px) skips pacing (engine already resolved — display catch-up only); settle markers (W/L/P chip) per bot seat.
- [ ] Build; suite+build green; e2e 22 stay green (update minimally if top-bar/strip text changed — note it). Commit `feat(c2): bot table UI`.

### Task 7: Profile editor Seats section (spec "UI")
**Files:** `src/ui/screens/ProfileEditor.tsx`, `src/ui/app.css`.
Seats section: playerHands segmented 1/2/3, bots stepper 0–5, mistake % stepper 0–20 step 1 (shown "Perfect" at 0), your-position stepper 0..bots (labeled "Seat N of M", clamped when bots changes).
- [ ] Build; suite+build+e2e green. Commit `feat(c2): seats config UI`.

### Task 8: Multi-hand bet entry + per-hand flow (spec "UI")
**Files:** `src/ui/screens/Table.tsx`, `src/ui/components/ActionBar.tsx`, `src/ui/app.css`.
When playerHands>1: bet phase collects one bet per hand (chip row per hand when betSpreadOn, else flat 1u each); active-hand highlight cycles across the player's hands; per-hand result lines.
- [ ] Build; suite+build+e2e green. Commit `feat(c2): multi-hand table flow`.

### Task 9: E2e + screenshots (spec "Testing")
**Files:** `e2e/table-seats.spec.ts` (+ helpers), screenshots.
Cases: full-table round (3 bots visible, seeded) with bot cards face-up + RC-consistent count-check; multi-hand round with two bets and independent results; fast-forward skips pacing; seats config via profile editor persists and takes effect (bot rows appear). Screenshots per distinct state; coordinator reviews personally. Run e2e twice green.
- [ ] Commit `test(c2): table seats e2e`.

## Self-Review
Spec coverage: Seat model→T1-3; multi-hand→T4-5,T8; UI→T6-8; testing→every task + T9. C2.5 insurance = T5 ✓. No placeholders; interfaces named with exact signatures; waves' file sets disjoint within each parallel group. Deferred by spec: bots that count, competition stats, cycle-3 audio.
