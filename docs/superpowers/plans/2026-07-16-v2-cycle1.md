# v2 Cycle 1 Implementation Plan — Payouts, Drill Controls, Variable Rules, Profiles, CVCX

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Insurance/payout correctness, in-drill controls + manual countdown, verified multi-ruleset strategy charts, Game Profiles, and CVCX ramp import — per spec `docs/superpowers/specs/2026-07-16-v2-cycle1-profiles-rules-design.md` (THE authority; every task's requirements = its spec section + this plan).

**Architecture:** All correctness in pure TS (`src/engine/`, `src/store/`); charts become per-ruleset assembled data under `src/engine/charts/`; profiles are a new storage entity with v1→v2 migration; UI stays dumb. TDD everywhere; every chart cell pinned against an independent transcription (v1 discipline).

**Tech Stack:** unchanged (Vite/React/TS strict/Vitest/Playwright).

## Global Constraints

- Spec sections override this plan on conflict; STOP and flag if they disagree.
- Strategy table data comes ONLY from coordinator-verified sources (the coordinator reviews Wizard of Odds chart images personally and supplies verified data in the dispatch). NEVER write chart cells from memory.
- verbatimModuleSyntax; strict TS; full `npm test` + `npm run build` green before every commit; commits end with `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- Parallel waves: tasks in the same wave touch DISJOINT file sets and run in isolated worktrees; do not touch files outside your task's list.
- v1 behavior parity: until C1.14 flips grading to profiles, default behavior must remain byte-identical (existing 612+ tests stay green untouched unless a task says otherwise).

## Waves (parallel-safe groupings)

- **Wave 1 (parallel ×3):** T1 (=C1.1+C1.2, engine payouts, `game.ts`+`game.test.ts`+payout audit) · T2 (=C1.3+C1.4, drill knobs/manual mode, `Drills.tsx`+`app.css`+`types.ts drill fields`) · T3 (=C1.15 parser core, new `src/store/cvcxParse.ts`+tests — pure module, no UI)
- **Wave 2 (serial):** T4 (=C1.5 chart module skeleton, wraps existing tables; touches `basicStrategy.ts`, `strategy.ts`, `game.ts` call sites)
- **Wave 3 (parallel ×3, data-only after T4):** T5 (=C1.6 4-8D S17) · T6 (=C1.7 2D H17+S17) · T7 (=C1.8 1D H17+S17) — each creates one data file + one test file under `src/engine/charts/`
- **Wave 4 (serial):** T8 (=C1.9 das/ls transforms) → T9 (=C1.10 rsa/bj65 gameplay) → T10 (=C1.11 S17 I18 variant)
- **Wave 5 (serial):** T11 (=C1.12 Profile store+migration) → T12 (=C1.13 profile picker/editor UI) → T13 (=C1.14 active-profile wiring + Stats CVCX header) → T14 (=C1.15 import dialog UI on T3's parser) → T15 (=C1.16 e2e + screenshots)

---

### Task 1: Insurance settlement + payout audit (spec §1)
**Files:** Modify `src/engine/game.ts`; Test `src/engine/game.test.ts` (+ new `src/engine/payouts.test.ts`)
**Interfaces:** Game gains `insuranceNet: number | null` (per round, reset each `startRound`); `insuranceDecision(take)` stakes `bet/2`, settles 2:1 on dealer BJ, loses stake otherwise; bankroll updated at settle time.
- [ ] Failing tests: four quadrants (take/decline × BJ/noBJ) asserting exact bankroll deltas — take+BJ non-BJ-hand: round net 0 (even-money wash); take+BJ player-BJ: +1×bet; take+noBJ: −bet/2 then round continues; decline paths unchanged. Payout audit table (spec §1): BJ 3:2, win/lose/push, double ±2, split sums, surrender −0.5, each vs hand-computed delta with rigged shoes.
- [ ] RED → implement → GREEN (full suite) → commit `fix: insurance settles (2:1) + payout audit table`

### Task 2: In-drill controls + manual countdown (spec §2)
**Files:** Modify `src/ui/screens/Drills.tsx`, `src/ui/app.css`, `src/store/types.ts` (drill gains `countManual: boolean` default false, `quizIndex: DeviationId|'all'` default 'all')
**Interfaces:** count-drill setup shows length/group/speed/mode inline (writes back via saveSettings); manual mode = no timer, one full-width tap target (≥60vh) advancing a group per tap, for both drill and countdown variants; flashcards inline category; quiz index filter (drawQuizItem gains optional `filter?: DeviationId` param — additive, default preserves behavior).
- [ ] Failing test (drills.test.ts): `drawQuizItem(seed, '16v10')` always returns that id; existing calls unaffected.
- [ ] Implement UI + param → suite green → commit `feat: in-drill controls, manual countdown, quiz index filter`

### Task 3: CVCX ramp parser (spec §5)
**Files:** Create `src/store/cvcxParse.ts`, `src/store/cvcxParse.test.ts`
**Interfaces:** `parseCvcxRamp(text: string, unitDollars?: number): { ok: true; rows: SpreadRow[] } | { ok: false; error: string }` — accepts 2-column TC/bet lines (tab/space/comma), optional header, `$` amounts (requires unitDollars else `{ok:false}` asking for it), `≤/+/TC` decorations, negative TCs; output sorted asc by minTc, deduped; ambiguity → line-numbered error, never guesses.
- [ ] Failing tests: unit table, $ table ÷ unit, all three separators, header skip, decorations, junk line → `error` contains line number, empty → error, unsorted input → sorted output.
- [ ] RED → implement → GREEN → commit `feat: CVCX ramp paste-parser`

### Task 4: Chart module skeleton (spec §3)
**Files:** Create `src/engine/charts/index.ts`, `src/engine/charts/d68_h17.ts` (move v1 tables verbatim); Modify `src/engine/basicStrategy.ts` (re-export/delegate), `src/engine/strategy.ts`, `src/engine/game.ts` (thread `rules`); Create `src/engine/ruleset.ts` (`RuleSet` + `DEFAULT_RULES` = v1 game); Test `src/engine/charts/charts.test.ts`
**Interfaces:** `getChart(rules: RuleSet): {HARD, SOFT, PAIRS}`; `chartLookup(cards, up, rules)`; `correctPlay(cards, up, tc, ctx, rules?)` (defaults DEFAULT_RULES → v1 parity); v1's 340-cell test keeps passing against `getChart(DEFAULT_RULES)`.
- [ ] Failing test: `getChart(DEFAULT_RULES)` deep-equals the v1 tables; unknown deck/s17 combos throw until later tasks land (explicit error listing available charts).
- [ ] Implement (pure move + threading; ZERO cell changes) → full suite green (612+) → commit `refactor: chart module keyed by RuleSet (v1 parity)`

### Tasks 5–7: Base tables — 4-8D S17 (T5), 2D H17+S17 (T6), 1D H17+S17 (T7)
**Files (each):** Create `src/engine/charts/<name>.ts` + `<name>.test.ts`; register in `charts/index.ts` (T5-7 edit disjoint data files; index registration lines are merge-trivial)
**Interfaces:** same `{HARD, SOFT, PAIRS}` shape as v1 tables (totals 4–21/13–21, 10 columns, ChartAction cells).
- [ ] Each: coordinator-supplied verified table (from WoO image reviewed at dispatch) transcribed to data file; test file carries an INDEPENDENT transcription typed from the dispatch (v1 T5 discipline: two encodings, every cell asserted); behavioral spot-checks per chart (e.g. 4-8D S17: 11vA H-not-Dh, 17vA S-not-Rs, A,7v2 S-not-Ds, A,8v6 S-not-Ds, 8,8vA P-not-Rp; 1D/2D cells per supplied data).
- [ ] RED → transcribe → GREEN → commit `feat: verified <name> base chart`

### Task 8: das/ls transforms (spec §3)
**Files:** Create `src/engine/charts/transforms.ts` + tests; Modify `charts/index.ts` (getChart applies transforms)
**Interfaces:** `applyDas(chart, das: boolean, deckClass)` — das:false replaces coordinator-supplied Ph-cells per deck class with fallback; ls handled at lookup (already v1 behavior via ctx) — transform only strips R-cells when `ls:false` so charts never advertise surrender: `applyLs(chart, ls)` mapping Rh→H, Rs→S, Rp→P.
- [ ] Failing tests: das:false 4-8D turns 2,2v2/3,3v2 (etc. per supplied delta list) into H; ls:false maps the three R-kinds; transform is pure (input chart unmutated); full combo matrix (6 bases × das × ls = 24) sanity: every cell is a legal ChartAction.
- [ ] RED → implement → GREEN → commit `feat: das/ls chart transforms`

### Task 9: rsa + bj65 gameplay (spec §3)
**Files:** Modify `src/engine/game.ts` + `game.test.ts`
- [ ] Failing tests: rsa:true allows re-splitting A,A to cap (rigged); rsa:false = v1; bj65 pays 1.2× (payout audit rows added); defaults unchanged.
- [ ] RED → implement → GREEN → commit `feat: rsa + 6:5 rules`

### Task 10: S17 Illustrious-18 variant (spec §3)
**Files:** Modify `src/engine/deviations.ts` (export `ILLUSTRIOUS_18_S17`), `src/engine/strategy.ts` (select by rules.s17); Test `strategy.test.ts`
**Interfaces:** S17 set = canonical values: 11vA active +1 double; 16v9 +5; 10vA +4; 12v6 −1 (dir lte); all others same as H17 set.
- [ ] Failing tests: threshold triples for the four changed entries under s17 rules; H17 behavior unchanged under default rules.
- [ ] RED → implement → GREEN → commit `feat: S17 index set, selected by ruleset`

### Task 11: Profile store + migration (spec §4)
**Files:** Create `src/store/profiles.ts` + tests; Modify `src/store/types.ts` (Profile, SeatConfig placeholder NOT included — cycle 2), `src/store/persist.ts` (new keys `bjtrainer.profiles.v1`, `bjtrainer.activeProfile.v1`)
**Interfaces:** `loadProfiles(): Profile[]` (≥1 guaranteed), `saveProfiles`, `getActiveProfile(): Profile`, `setActiveProfile(id)`; migration: if no profiles key and v1 settings exist → build "Default (6D H17)" from settings' game fields (betSpreadOn, spread, bankrollStart, countCheckEvery, penetration) + DEFAULT_RULES; idempotent; corrupt → defaults.
- [ ] Failing tests: migration from a captured v1 settings blob (exact expected profile), idempotency (run twice = once), corrupt/missing → default profile, active-id fallback when pointing at deleted profile.
- [ ] RED → implement → GREEN → commit `feat: game profiles + v1 migration`

### Task 12: Profile picker + editor UI (spec §4)
**Files:** Create `src/ui/screens/ProfileEditor.tsx`; Modify `src/ui/screens/Home.tsx` (active-profile button → picker), `src/ui/App.tsx` (screen: 'profiles'), `app.css`
Behaviors: picker lists profiles (select/add/duplicate/edit/delete-confirm, last undeletable); editor = name, rules toggles (decks segmented 1/2/6/8, S17/H17, DAS, LS, RSA, 6:5), penetration stepper, ramp editor (reuse spread editor), bankroll/unit$, count-check stepper, betSpreadOn, CVCX fields (score/evPerHour/riskOfRuin/simNote); saves via profiles store; ≥48px controls, dark theme.
- [ ] Build → suite+build green → commit `feat: profile picker + editor`

### Task 13: Active-profile wiring + Stats header (spec §4)
**Files:** Modify `src/ui/useGame.ts` (GameConfig from active profile incl. rules), `src/engine/game.ts` (accept rules in config; chart/index selection via T4/T10), `src/ui/screens/Table.tsx`, `Drills.tsx` (grading ctx per profile rules), `Stats.tsx` (per-profile header: CVCX numbers + play-accuracy% + units/hr proxy labeled "assumes 80 rounds/hr"), `src/store/stats.ts` (sessions gain `profileId`)
- [ ] Failing engine test: Game with s17 rules → dealer stands A,6; chartLookup routed by rules. UI wiring manual-verify + build; sessions carry profileId (unit test).
- [ ] Implement → suite green → commit `feat: everything grades against the active profile`

### Task 14: CVCX import dialog (spec §5)
**Files:** Modify `src/ui/screens/ProfileEditor.tsx` (+css)
Behaviors: "Paste from CVCX" → textarea → parse via T3 → preview table (TC/units) → Confirm replaces ramp / error shown with line number; asks unitDollars when parser demands it.
- [ ] Build → green → commit `feat: CVCX ramp import dialog`

### Task 15: E2e + screenshots (spec §6)
**Files:** Modify `e2e/*.spec.ts` (+ new `e2e/profiles.spec.ts`)
Cases: create S17 profile → switch → seeded round where dealer holds A,6 and STANDS; manual countdown tap-through; paste-import happy + error; quiz index filter; migration (bootstrap v1-style localStorage → default profile appears). Screenshots of new screens; coordinator reviews personally.
- [ ] Specs green ×2 → full suite + build → commit `test: cycle-1 e2e`

## Self-Review
Spec coverage: §1→T1; §2→T2; §3→T4-T10; §4→T11-T13; §5→T3+T14; §6→T15 + per-task tests. Placeholders: chart data intentionally dispatch-supplied (coordinator-verified, per Global Constraints — not implementer-guessed). Type consistency: RuleSet (T4) consumed T5-13; parseCvcxRamp (T3) consumed T14; Profile (T11) consumed T12-13. Waves' file sets verified disjoint.
